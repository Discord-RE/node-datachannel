module.exports = {
  // This must match the output file as specified in CMakeLists.txt
  // (project name `node_datachannel` -> `node_datachannel.node`)
  name: 'node_datachannel',
  // The node-api versions that are built
  napi_versions: [8],
};
